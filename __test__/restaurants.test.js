const {restaurants} = require("../controllers/restaurantsControllers.js")

describe("Restaurants", ()=>{
    test("get an array of objects with restaurant information", () => {
        expect(1).toBe(1);
    });
});